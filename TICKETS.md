# TICKETS — Купол v0.1 «Анализ»

Статусы: `todo` · `doing` · `done` · `blocked`. Порядок внутри эпика — рекомендуемый порядок выполнения. Ссылки на спеку — `SPEC.md` (§15 имеет приоритет). Все DSP-тикеты закрываются только с тестами на синтетике.

Критерий версии 0.1 (тег `v0.1.0`): экран «Анализ» по §15.3 стабильно работает 10 минут на KENSHI E11 без дропов кадров, калибровка и сессия с очками работают, RU/EN, светлая/тёмная тема, портрет/ландшафт.

---

## E0 · Инфраструктура

### T-001 · Инициализация репозитория `done`
- `git init`, `.gitignore` (Gradle/Android/IDE), `LICENSE` (MIT), `README.md` (описание, ограничения §13, «не медицинский инструмент»).
- Первый коммит: SPEC, RESEARCH, CLAUDE.md, TICKETS.md, design/.
- **Готово, когда:** `git log` содержит initial commit; README описывает, как собрать.

### T-002 · Gradle-каркас, модули `done` · после T-001
- Gradle KTS, version catalog (`libs.versions.toml`): AGP 8.x под Gradle 8.11, Kotlin 2.x, Compose BOM, compileSdk 35, minSdk 26, targetSdk 35, JDK 17.
- Модули: `:app`, `:core-audio`, `:core-dsp` (jvm), `:core-notation` (jvm), `:core-testdata` (jvm). В `:core-dsp`/`:core-notation`/`:core-testdata` — `kotlin("jvm")`, без Android-плагина; в конфигурации запрещён импорт `android.*`.
- `applicationId = ru.dvedev.me.cupola`, `versionName 0.1.0`.
- **Готово, когда:** `./gradlew :core-dsp:test :app:assembleDebug` проходит на сервере; пустое приложение ставится на E11.
- Итог 2026‑09‑15: AGP 8.7.3 / Gradle 8.11.1 / Kotlin 2.1.0 / Compose BOM 2024.12.01; запрет `android.*`/`androidx.*` в JVM-модулях — задача `checkNoAndroidImports` в корневом `build.gradle.kts`, входит в `check`.

### T-003 · `scripts/deploy.sh` `done` · после T-002
- `rsync` (исключая `build/`, `.gradle/`) → `root@217.60.62.102:/root/cupola`; `./gradlew :app:assembleDebug` там; `scp` APK в `build/`; `adb -s 192.168.0.16:5555 install -r`; опционально `am start`.
- Флаги: `--test` (только `:core-dsp:test`), `--no-install`, `--logcat` (хвост логов пакета после запуска).
- **Готово, когда:** одна команда от правки до запущенного приложения на планшете; время цикла измерено и записано в README.
- Итог 2026‑09‑15: добавлены `--check` (`gradlew check`) и `-- <args>` (проброс аргументов Gradle). Время при тёплом демоне: полный цикл до запущенного приложения — 61 с, `--test` — 6 с, `--check` — 56 с (lint). Первый запуск с загрузкой Gradle и зависимостей — ~5 мин.

### T-004 · Дизайн-токены и шрифты `done` · после T-002
- Compose-тема: цвета §15.4 (светлая/тёмная), типографика Manrope + IBM Plex Mono (вшить, OFL), размеры бейджей/подписей.
- Формы: без скруглений внутри экрана, разделители 1 px, шапки зон `panel2` 60 %.
- **Готово, когда:** превью-экран показывает все токены в обеих темах; переключение темы в рантайме без перезапуска.
- Итог 2026‑09‑15: `ui/theme/` (`CupolaColors`, `CupolaTypography`, `CupolaDimens`, `SpectrogramColormap` — LUT как в макете, `CupolaTheme` + маппинг на Material 3), `ui/components/Primitives.kt` (`ZoneHeader`, `ZoneDivider`, `Badge`, `Label`, `PillButton`, `Segmented`), `ui/preview/TokensPreviewScreen` (пока контент `MainActivity`; после T-050 — из настроек). Manrope — переменный TTF, ось `wght` задаётся через `FontVariation` явно (иначе грузится дефолтный инстанс). Проверено на E11 в обеих темах, переключение живое.

---

## E1 · `:core-notation`

### T-010 · Ноты, октавы, центы `done` · после T-002
- `hzToMidi(f, a4)`, `midiToHz`, `nearestNote`, `cents` ∈ (−50, +50]; A4 настраиваемый 415–466.
- Имена: EN научные (`C4`, `C♯4`, бемоли по настройке); RU компактные (`Соль¹`, `Ля малой`, `До большой`, суб-/контроктава) и полные («ля первой октавы») для accessibility.
- Форматтеры `NotationMode = RU | EN | BOTH`.
- **Тесты:** 440 → A4/Ля¹ 0 ¢; 261.63 → C4/До¹; 226 → A3 +46 ¢; границы октав (B3/C4); a4 = 432.
- **Готово, когда:** таблица §7 полностью покрыта тестами.
- Итог 2026‑09‑15: `Pitch.kt` (`hzToMidi`, `midiToHz`, `centsBetween`, `nearestNote` → `Pitch(note, cents)`, `Tuning` 415–466), `Note.kt` (`Note` value class, `PitchClass`, `Accidentals`), `NoteNames.kt` (`en`/`enAscii`/`ru`/`ruFull`/`label`/`cents`/`centsSpoken`, `NotationMode`, `NoteLabel`). 16 тестов, включая всю таблицу октав §7.

---

## E2 · `:core-dsp`

### T-020 · Кадрирование и окно `done` · после T-002
- `FrameSource`: из потока сэмплов → кадры 2048 (или 4096) с hop 480 @ 48 кГц (соответственно при 44.1 кГц), окно Hann, float.
- Lock-free ring buffer (single producer / single consumer) без аллокаций в горячем пути.
- **Тесты:** число кадров на N сэмплов; отсутствие пропусков при рваной подаче.
- Итог 2026‑09‑15: `frame/Framer` (кадры `Frame.raw`/`windowed` без аллокаций, `centerSeconds`/`endSeconds`), `frame/FloatRingBuffer` (SPSC, счётчик `overruns`), `frame/Windows`. 7 тестов, включая многопоточный.

### T-021 · FFT и спектр мощности `done` · после T-020
- Собственная radix-2 real-FFT (2048/4096), power spectrum, dB (`10·log10`), без аллокаций на кадр.
- **Тесты:** сравнение с наивным DFT (ошибка < 1e-6); синус 1 кГц → пик в правильном бине; производительность: 2048-точечная FFT < 0.3 мс на JVM (ориентир).
- Итог 2026‑09‑15: `fft/RealFft` (упакованная real-FFT через комплексную N/2, double), `fft/PowerSpectrum` (power + dBFS, нормировка «синус амплитуды 1 → 0 dB», `energy`, `peakDb`, `refinePeakHz`). 0.035 мс/кадр на сервере.

### T-022 · `PitchDetector` и YIN `done` · после T-020
- Интерфейс `PitchDetector { fun estimate(frame): PitchEstimate(f0, confidence) }`.
- YIN: difference function, CMND, порог 0.1–0.15, параболическая интерполяция, `confidence = 1 − cmnd(τ)`; окно 2048–4096; диапазон 60–1200 Гц.
- **Тесты (§10):** синус 220 Гц → 220 ± 0.5 Гц (≤ 5 ¢); 82 Гц (E2) и 1047 Гц (C6) без октавных ошибок; белый шум → confidence < 0.5; глиссандо — отслеживается.
- **Готово, когда:** ≤ 5 ¢ на устойчивом синтетическом гласном, латентность оценки < 5 мс на JVM.
- Итог 2026‑09‑15: `pitch/PitchDetector` + `PitchEstimate`, `pitch/YinPitchDetector` (порог 0.12, окно корреляции `frame − τmax`, параболическая интерполяция). 7 тестов: 220 Гц ±0.5, E2/C6 без октавных ошибок, гласный с формантами < 5 ¢, шум conf < 0.5, глиссандо; 1.5 мс/кадр.

### T-023 · Гармоники, обертоны, noise floor `done` · после T-021, T-022
- Noise floor: RMS-порог (noise + 10 dB) и по-биновый профиль (10-й перцентиль, медленное обновление; инициализация 1 с).
- Harmonic tracker: для k = 1..K (до 8 кГц) пик в `k·f0 ± 3 %`, уровень `H_k`; «слышимый» = `H_k ≥ noiseFloor(k·f0) + 12 dB` и `H_k ≥ H_1 − 50 dB`; `overtoneCount` (k ≥ 2), медиана 300 мс; настройка «включая основной тон».
- **Тесты:** пила 200 Гц + шум — гармоники на k·f0, count растёт при падении шума; синус → count = 0.
- Итог 2026‑09‑15: `metrics/NoiseFloor` (RMS — трекер минимума с подъёмом 0.5 dB/с; профиль по бинам — 10‑й перцентиль невокализованных кадров, инициализация 1 с), `metrics/HarmonicTracker` + `HarmonicSet` (окно ±3 %, но не шире 40 % от f0 — иначе при k > 16 цепляется сосед). Медиана 300 мс — `util/RollingMedian` на стороне вызывающего.

### T-024 · Метрики купола `done` · после T-023
- Полоса по типу голоса (§5.3) или пользовательская `[lo, hi]`.
- `RingRatio_dB`, `RingShare_%`, `PeakSPR_dB`, `SPL` (RMS dBFS), `RingRatio_norm = RingRatio − k·(SPL − SPL_baseline)`, `k = 0.7`.
- **Тесты:** одинаковый спектр при разной громкости → `RingRatio_norm` в пределах 1 dB; +10 dB в полосе 2.7 кГц → RingRatio +10 ± 1 dB; смена полосы меняет результат ожидаемо.
- Итог 2026‑09‑15: `metrics/RingMetrics` (`measure` → `RingMeasure`, `normalise`, `splDbfs`), `VoiceType` с полосами §5.3, `RingBand` (ширина ≥ 200 Гц). Уточнение теста: «одинаковый спектр при разной громкости» = голос на +10 dB SPL с +7 dB в полосе (Bloothooft & Plomp) → norm совпадает в пределах 1 dB; чистое усиление по определению снижает norm на k·ΔSPL — отдельный тест.

### T-025 · Статистика высоты `done` · после T-022
- Контур центов; скользящая медиана 200 мс (медианная линия); `pitchSD` за 500 мс по медианной линии; `drift` — наклон регрессии за 2 с (¢/с).
- **Тесты:** вибрато 5.5 Гц ±70 ¢ → pitchSD < 10; глиссандо +50 ¢/с → drift 50 ± 5.
- Итог 2026‑09‑15: `metrics/PitchStats` (абсолютные центы, медиана 200 мс + скользящее среднее 200 мс от неё, чтобы убрать рябь медианы при некратном периоде вибрато; `pitchSd`, `drift`, `residual`; сброс после паузы ≥ 200 мс). При вибрато ±70 ¢ pitchSD = 1.2 ¢.

### T-026 · Вибрато `done` · после T-025
- Контур минус медианная линия → FFT/автокорреляция за 1–2 с → `vibRate`, `vibExtent`; классификация: вибрато 4.5–7 Гц и ≤ ±120 ¢; качание < 4 Гц или > ±120 ¢; тремоло > 7.5 Гц; прямой тон < ±15 ¢.
- **Тесты:** 5.5 Гц ±70 ¢ → rate 5.5 ± 0.3, extent 70 ± 10; прямой тон; качание 3 Гц ±150 ¢.
- Итог 2026‑09‑15: `metrics/VibratoAnalyzer` (окно 2 с, Hann + FFT 1024 по остатку, пик 1.5–15 Гц с параболическим уточнением, extent — амплитуда пика; пересчёт каждые 5 кадров), `VibratoKind` NONE/STRAIGHT/VIBRATO/WOBBLE/TREMOLO; зазоры 4–4.5 и 7–7.5 Гц отнесены к VIBRATO.

### T-027 · Гейты, показатели, score `done` · после T-024, T-025, T-026
- Показатели `ring`, `pitch`, `steady` (0…1) по §6.2.1 с поправкой §15.5 (качание/тремоло снижают `steady`); `score = 0.6·ring + 0.25·pitch + 0.15·steady`; таблица весов — одно место с пометкой «до CPPS».
- Гейты: голос, `confidence ≥ 0.7`, `pitchSD ≤ 20`, `SPL ≤ baseline + 12`; при срабатывании — код гейта для подсказок.
- Сглаживание `attackRelease(80 мс, 400 мс)`; серия ≥ 3 с при `score ≥ 0.7`.
- **Тесты:** каждая ветка гейта; монотонность score по ring; `pushed` при SPL + 13 dB → score 0.
- Итог 2026‑09‑15: `score/ScoreWeights` (0.6/0.25/0.15, единственное место), `Gate` (OPEN, NO_VOICE, LOW_CONFIDENCE, PUSHED, UNSTABLE_PITCH, SOVT, NOT_CALIBRATED — порядок = приоритет подсказок), `ScoreParams`, `Scorer` (`ScoreInput` → `ScoreOutput`), `AttackRelease`. Серия считается по сглаженному score, поэтому одиночный невокализованный кадр её не рвёт (release 400 мс мостит согласные), 0.5 с тишины — рвёт. Качание/тремоло × 0.6 на `steady`.

### T-028 · Калибровка (baseline) `done` · после T-024
- Процедура: тишина 2 с → noise floor; /а/ 4 с → `RingRatio_baseline`, `SPL_baseline`; валидация: шум > −45 dBFS → предупреждение; доля кадров с `confidence < 0.7` > 50 % → «повторить».
- Модель `Calibration(voiceBand, noiseFloorDb, ringRatioDb, splDbfs, createdAt)`; хранение — в `:app` (DataStore/JSON), ключ — тип голоса/диапазон.
- **Тесты:** синтетическая калибровка даёт ожидаемые baseline; валидация срабатывает.
- Итог 2026‑09‑15: `calibration/Calibration` (band, noiseFloorDbfs, ringRatioDb, splDbfs, voicedShare, createdAtEpochMs), `CalibrationSession` (фазы SILENCE 2 с → VOWEL 4 с → DONE, `progress`/`secondsLeft`, медианы; `NOISY_ROOM` при шуме > −45 dBFS, `UNSTABLE_VOICE` при доле вокализованных < 50 % → `mustRepeat`). Хранение — в `:app` (T-060/T-061).

### T-029 · Кадры сессии и агрегаты `done` · после T-027
- `FrameMetrics` (публикуется 100 Гц) и `SessionFrame` (децимация 10 Гц: t, f0, cents, ringNorm, spl, score, gate).
- `SessionSummary`: средний/лучший score, доля времени ≥ 0.7, очки, лучшая серия, средние по ring/pitch/steady. Модель `SessionRecording` (кадры + ссылка на WAV) — только тип, без записи на диск.
- Начисление очков: интервал `0.6 − 0.48·ring` с при `score ≥ 0.5`, порция `1 + round(2·ring)`.
- **Тесты:** сводка по известной последовательности кадров; счётчик очков по таймлайну.
- Итог 2026‑09‑15: `FrameMetrics` (в корне `dsp`), `session/SessionFrame`, `SessionSummary` (средние — по вокализованным кадрам), `SessionRecording`, `SessionAccumulator` (децимация 10), `PointsCounter` (первая порция через интервал после входа в score ≥ 0.5).

### T-030 · `:core-testdata` `done` · после T-002
- Генераторы: синус, пила, гармонический голос (тилт, форманты, полоса купола с усилением), вибрато, глиссандо, белый/розовый шум, огибающая атаки; запись WAV для ручной проверки.
- **Готово, когда:** все тесты E2 используют только эти генераторы.
- Итог 2026‑09‑15: `Signals` (sine, sawtooth, harmonicVoice с тилтом/формантами, white/pink noise, mix/concat/gain/envelope, rms), `PitchContour` (Constant/Vibrato/Glissando/Custom), `Formant`, `VoiceSpec`, `Wav.write`.

### T-031 · Прогон §10 и бенчмарк `done` · после T-021…T-029
- Единый набор тестов §10 + микробенчмарк полного конвейера (кадр → метрики) на JVM; цель < 2 мс/кадр.
- Итог 2026‑09‑15: `Analyzer` + `AnalyzerConfig` — весь конвейер §4 (кадр → спектр → YIN → гармоники → метрики → score → `FrameMetrics`, живой `PowerSpectrum` отдаётся в колбэке для спектрограммы); `Spec10SuiteTest` сквозь `Analyzer`. Бенчмарк на сервере: 1.59 мс/кадр (из них YIN ≈ 1.5). Всего в `:core-dsp` 66 тестов.

---

## E3 · `:core-audio`

### T-040 · Захват `AudioRecord` `done` · после T-002
- Источник: `UNPROCESSED` (проверка `PROPERTY_SUPPORT_AUDIO_SOURCE_UNPROCESSED`) → fallback `VOICE_RECOGNITION`; ручной выбор; отчёт `AudioSourceStatus` (какой источник, «обработка отключена / не гарантирована»). Никогда `MIC`/`DEFAULT`, без `NoiseSuppressor`/`AGC`.
- 48 кГц (fallback 44.1), mono, float или 16-bit; буфер `getMinBufferSize() × 4`.
- **Готово, когда:** на E11 в логе видно выбранный источник и sample rate; спектр синуса с мониторов без «дыр» в 2–4 кГц.
- Итог 2026‑09‑15: `audio/AudioCapture` (`AudioRecord.Builder`, float mono, 48k→44.1k, буфер ×4; AUTO = UNPROCESSED только если задекларирован, иначе VOICE_RECOGNITION — т.к. незадекларированный UNPROCESSED ведёт себя как DEFAULT), `AudioSourcePreference`, `AudioSourceStatus` + `ProcessingState` (DISABLED / NOT_GUARANTEED / UNVERIFIED). E11: `PROPERTY_SUPPORT_AUDIO_SOURCE_UNPROCESSED = false` → VOICE_RECOGNITION @ 48000, буфер 80 мс. Проверка спектра на «дыры» — T-070.

### T-041 · Поток чтения и доставка кадров `done` · после T-040, T-020
- Отдельный поток `THREAD_PRIORITY_URGENT_AUDIO`, чтение в ring buffer из T-020, без аллокаций; счётчик переполнений/дропов.
- Публикация `StateFlow<FrameMetrics>` (100 Гц) из `AnalysisEngine`, объединяющего T-02x.
- **Готово, когда:** 10 минут без дропов на E11; латентность микрофон → метрика ≤ 60 мс (замер: щелчок → отметка).
- Итог 2026‑09‑15: `audio/AudioEngine` — поток захвата (`URGENT_AUDIO`) → `FloatRingBuffer` (1 с) → поток анализа (`Analyzer`), `StateFlow<FrameMetrics?>` 100 Гц + `FrameListener` с живым спектром, счётчики `overruns`/`readErrors`, `processingLatencyMs` (E11: ~4 мс от конца чанка до метрики; +80 мс буфер AudioRecord — акустический замер в T-070). 10‑минутный прогон — T-070.

### T-042 · Foreground-service `done` · после T-041
- Сервис с `foregroundServiceType="microphone"`, уведомление «Купол слушает · <нота> · [Стоп]»; запускается только при активной сессии; без сессии сворачивание останавливает захват.
- **Готово, когда:** сессия продолжается при переключении в другое приложение; нота в уведомлении обновляется; «Стоп» закрывает сессию.
- Итог 2026‑09‑15: `service/AnalysisService` (тип `microphone`, канал «Сессия», обновление ноты раз в секунду, action «Стоп» → `AppGraph.stopSession`). Запуск/остановка — из `AppGraph.startSession/stopSession`; без сессии `ON_STOP` останавливает движок (`activityVisible`). Проверено на E11: `isForeground=true`, уведомление в шторке, микрофон не прерывается.

---

## E4 · `:app` — экран «Анализ»

### T-050 · Скелет приложения `done` · после T-004
- Навигация: онбординг → анализ → настройки; ручной `AppGraph` (DI); `AnalysisViewModel` на `AnalysisEngine`; разрешение микрофона inline.
- Состояние 2026‑09‑15: `CupolaApp`/`AppGraph`, `AnalysisViewModel` (`uiMetrics` с сэмплированием 25 Гц, `SpectrogramHistory`/`SpectrumSnapshot`/`SessionController` как `FrameListener`), запрос микрофона, старт/стоп анализа по lifecycle. Навигация онбординг/настройки — вместе с T-059/T-060.

### T-051 · Шапка `done` · после T-050
- Слева `Купол · <тип> · <полоса>`; справа время, очки (золотом), «Пауза» (контур), «Старт/Стоп» (заливка / приглушённый контур), шестерёнка. Ландшафт — во всю ширину.
- Итог 2026‑09‑15: `ui/analysis/TopBar.kt`; «Пауза» видна только в сессии; на узких экранах (< 420 dp) тип/полоса скрываются.

### T-052 · Зона «Нота» `done` · после T-050, T-010
- Нота крупно (RU/EN/обе), центы цветом (≤ 10 ok, ≤ 25 warn, > 25 bad; пороги из настроек), шкала −50…+50 с зелёной зоной и тиками ±25, маркер; строка `f0 · обертонов N · вибрато/качание/тремоло/прямой тон · цель`; дуга «купол» с числом; строка подсказки постоянной высоты; зелёное наложение ∝ score; тап по ноте — закрепить/снять цель; долгий тап по дуге — калибровка.
- **Готово, когда:** появление подсказки ничего не сдвигает; при тишине — «—», индикаторы гаснут.
- Итог 2026‑09‑15: `ui/analysis/NoteZone.kt`. Заголовок — `NoteNames.ruShort` (Гельмгольц: ля / Ля / Ля¹ / Ля₁), т.к. «Ля малой» не влезает в 84 sp; в ландшафте (`compact`) дуга под нотой. Долгий тап по дуге → колбэк калибровки (экран — T-061).

### T-053 · Спектрограмма `done` · после T-050, T-023
- `Bitmap` ring buffer 800×N столбцов (10 мс/столбец), лог-ось 80 Гц–8 кГц, авто-нормализация по максимуму за 3 с (диапазон 60 dB), колормап §15.4 (обе темы), сглаженная отрисовка; гуттеры: частоты слева, тики гармоник с `k ≤ 16` справа, ось времени снизу; лента купола с пунктиром и подписью; линия f0 цветом попадания; линия цели + гармоники (30 %).
- Буфер 60 с в памяти; в паузе — скролл назад.
- **Готово, когда:** 60 fps на E11 при 100 столбцах/с; переключение лог/лин и 2048/4096 без перезапуска.
- Состояние 2026‑09‑15: `analysis/SpectrogramHistory` (6000 × 320, лог-строки, нормализация по максимуму 3 с, пишется из потока анализа) + `ui/analysis/SpectrogramZone` (кольцевой `Bitmap` 800 столбцов, инкрементальная догрузка на каждом кадре дисплея, оси, лента купола, трек f0 с разрывом при октавных скачках, линии цели, тики k ≤ 16 без наложений). Осталось: скролл в паузе (T-056), лог/лин и 4096 (после T-060), замер fps (T-070).

### T-054 · Спектр `done` · после T-050, T-023
- Лог-ось X, dB по Y с сеткой, полоса купола, кривая с заливкой, сглаженная огибающая по гармоникам, номера над пиками без наложений, пунктир noise floor; в шапке `купол ±X dB к калибровке`.
- Итог 2026‑09‑15: `analysis/SpectrumSnapshot` (двойной буфер) + `ui/analysis/SpectrumZone`; шкала dB следует за нормализацией спектрограммы (70 dB вниз от максимума); интерполяция между бинами на низких частотах.

### T-055 · Сессия, очки, подсказки `done` · после T-051, T-052, T-029
- Старт (проверка калибровки → предложить), стоп → диалог сводки; начисление очков с анимацией кружков из дуги (золото / зелёные при ≥ 0.85, размер ∝ ring), отключаемая; серии; подсказки `hint.pushed`/`hint.drift`/`hint.good` по приоритету, не чаще раза в 3 с, тексты в `strings.xml` (`hint.*`).
- Итог 2026‑09‑15: `analysis/SessionController` (часы, очки через `SessionAccumulator`, серия, подсказки с троттлингом 3 с, публикация ~20 Гц), `PointsBurst` в `NoteZone`, диалоги «Нужна калибровка» и «Сессия» в `AnalysisScreen`; ключи строк — `hint_pushed/hint_drift/hint_good` (в Android-ресурсах точки в именах недопустимы).

### T-056 · Пауза `done` · после T-053
- Кнопка «Пауза/Дальше»: заморозка графиков и readouts, скролл спектрограммы по 60-секундному буферу, анализ в фоне продолжает писать буфер; метка «ПАУЗА».
- Итог 2026‑09‑15: `AnalysisViewModel.togglePause/scrollBy` (`frozenMetrics`, `pausedHead`, `viewEnd`), `SpectrumSnapshot.freeze`, горизонтальный drag в `SpectrogramZone`, абсолютные подписи времени при прокрутке; пауза работает и вне сессии (в сессии — замораживает часы и очки).

### T-057 · Гаптика `done` · после T-055
- `VibratorManager`; амплитудный режим (сегменты 100 мс, `30 + 225·score`, начало/конец с 0) при `hasAmplitudeControl`; иначе импульсы 40 мс с периодом `600 − 500·score`; по умолчанию вкл. на телефонах (`sw < 600 dp`), выкл. на планшетах; предупреждение про подставку в онбординге.
- Итог 2026‑09‑15: `haptics/HapticsController` (цикл на `AppGraph.scope`, только в активной незапаузенной сессии, score ≥ 0.2). На E11 (без amplitude control, вибрация по умолчанию выкл.) физически не проверялась — см. T-070.

### T-058 · Ландшафт `done` · после T-051…T-054
- Шапка во всю ширину; колонка «Нота» 360 dp слева; справа спектрограмма ~60 % / спектр ~40 %; всё влезает без прокрутки на 1333×800 dp; портрет остаётся основным.
- Итог 2026‑09‑15: `ui/analysis/AnalysisScreen.kt` (`BoxWithConstraints`: ландшафт при ширине > высоты и ≥ 600 dp). Проверено на E11 в обеих ориентациях.

### T-059 · Онбординг `done` · после T-050, T-060
- Микрофон → экран «всё относительно, сравнивай с собой» → тип голоса (список + «не задан») → калибровка (T-061). Пропуск калибровки допустим, но «Старт» её потребует.
- Итог 2026‑09‑15: `ui/onboarding/OnboardingScreen` (4 шага; шаг микрофона пропускается, если разрешение уже есть), флаг `onboardingDone` в настройках.

### T-060 · Настройки `done` · после T-050
- DataStore; список §15.5 (тип голоса + свой диапазон [lo, hi] + кнопка «Калибровать», A4, нотация, язык, тема, шкала, окно FFT, источник аудио + индикатор, вибрация, анимация очков, подсказки; свёрнутый Advanced: пороги центов, confidence, `k`, веса, «сбросить»); у каждого поля «?» с пояснением (`glossary.*`/`settings.*.help` в `strings.xml`).
- Итог 2026‑09‑15: `settings/Settings` + `SettingsRepository` (DataStore Preferences, калибровки JSON по ключу `voice:<тип>` / `band:<lo>-<hi>`), `ui/settings/SettingsScreen` + компоненты (`SettingRow` с «?», `ChoiceRow`, `SwitchRow`, `StepperRow`); ключи пояснений — `settings_<поле>_help`. Смена FFT перезапускает движок; смена источника — тоже; язык — через `attachBaseContext` + `recreate()`.

### T-061 · Экран калибровки `done` · после T-028, T-050
- Шаги «тишина 2 с» → «/а/ 4 с» с обратным отсчётом, живым уровнем и спектром; предупреждение о шуме, повтор при нестабильном голосе; сохранение по типу/диапазону; вход из настроек, из онбординга и по долгому тапу на дуге.
- Итог 2026‑09‑15: `calibration/CalibrationViewModel` + `ui/calibration/CalibrationScreen` (интро → тишина → «а» → итог; уровень с меткой −45 dBFS, прогресс, живой спектр внизу). Проверено на E11 с тоном 440 Гц.

### T-062 · Локализация RU/EN `done` · после T-052…T-061
- Все строки в `strings.xml` (`values`, `values-en`), включая подсказки, глоссарий и тексты «?»; язык по системной локали + переключатель.
- Итог 2026‑09‑15: RU и EN полные для всех экранов v0.1; переключатель «Язык» (система / русский / English). Галерея токенов (dev) остаётся на русском.

---

## E5 · Проверка и релиз

### T-070 · Стенд и проверки на E11 `done` · после T-041, T-053
- Проверить `UNPROCESSED` vs `VOICE_RECOGNITION` на E11 (спектр синуса/свипа с мониторов), латентность ≤ 60 мс, 10 минут без дропов, низкие мужские ноты (~80 Гц) без октавных ошибок, C6 держится, вибрация на столе vs в руке.
- Результаты — в `docs/device-notes.md`.
- Итог 2026‑09‑15: 10 минут — 59 990 кадров, 0 overruns/readErrors, задержка обработки ≤ 20 мс, память стабильна; рендер оптимизирован с 62 % до 13 % janky при 60 fps. **Не сделано** (нужна тихая комната): сравнение UNPROCESSED/VOICE_RECOGNITION по свипу, акустический замер задержки, тоны E2/C6 с мониторов (покрыты JVM-тестами), физическая проверка вибрации. Подробности — `docs/device-notes.md`.

### T-071 · Релиз 0.1.0 `done` · после всех
- README: ограничения §13, инструкция сборки; `CHANGELOG.md`; тег `v0.1.0`; debug/release APK в `build/`.
- Итог 2026‑09‑15: README и `CHANGELOG.md` обновлены; `scripts/deploy.sh --release` собирает `app:assembleRelease` (подпись debug-ключом, без минификации) → `build/cupola-release.apk`; тег `v0.1.0` на финальном коммите.

---

## Backlog (не v0.1)

- B-01 Запись WAV + «поделиться» (SAF); B-02 «История» (Room, список сессий, графики прогресса, сравнение записей, LTAS); B-03 Advanced-панель (CPPS, HNR, H1−H2, Tilt) и `clean` в score; B-04 Адаптивные фильтры «как в FPV» (динамические notch на стационарные пики, бланкирование окон вибро); B-05 Экспорт сессии (кадры + WAV) для учителя, импорт/просмотр; B-06 Режимы «Тренировка», «Удержание», «Разминка/SOVT», тест диапазона; B-07 MPM/pYIN, CREPE-tiny/SPICE; B-08 Публикация: GitHub, CI (тесты + APK по тегу), F-Droid; B-09 Режим фидбека «Итог»; B-10 Пер-гласная калибровка; B-11 Дневная нагрузка (`voicedSeconds`, `cycleDose`).
