/**
 * `:core-testdata` — synthetic signal generators (sine, harmonic series, vibrato,
 * noise, glissando, breathiness) used by the SPEC §10 test suite. Pure JVM.
 * Implementation lands in T-030.
 */
package ru.dvedev.me.cupola.testdata
