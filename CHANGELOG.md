# Changelog

## 0.2.9 — в работе

- Обертоны закреплённой ноты тусклее: 35 % у первого, нелинейно до 10 % к 7‑му, дальше растворяются к 15‑му.
- «Сбросить» → «Сбросить все настройки» (с подтверждением; язык сохраняется); футер настроек отделён разделителем.

- Под шкалой центов: частота · общий уровень голоса в dB (за вычетом фонового шума, чтобы сравнивать с dB купола) · вибрато. Обертоны и цель из этой строки убраны.
- Диапазон частот спектрограммы и спектра настраивается в тонких настройках («Частоты: от / до», по умолчанию 60–8000 Гц); на лог-оси подписана и нижняя граница.

## 0.2.8 — 2026‑09‑16

- «Подстройка строя» убрана — осталась одна настройка «Строй A4». Линии закреплённой ноты на спектрограмме теперь тоже строятся от неё (раньше всегда от 440 Гц); определение ноты и «Дать тон» уже учитывали A4.
- Номера гармоник на спектре доходят до 15: при нехватке места пропускается средний номер, а не последний.

- Линии закреплённой ноты на спектрограмме: основной тон 2 dp, обертоны вдвое тоньше, от 50 % прозрачности с шагом −3 %, так что 15‑й обертон растворяется. Номера гармоник на спектре и спектрограмме — с 0 (основной тон) до 15.
- Настройки: кнопка «Палитра» и экран токенов удалены; внизу — ссылка на проект на GitHub и просьба поддержать разработку.
- README переписан под текущую версию; добавлен README-EN.md.

## 0.2.7 — 2026‑09‑16

- Фоновый шум: окно по умолчанию 3 минуты тишины, настраивается в тонких настройках («Окно шума», 1–10 мин).
- Сглаживание показаний: бегунок центов, центы и частота усредняются за последние 100 мс (тонкие настройки, 0–1000 мс); бегунок плавно едет между обновлениями.
- Микрофон: «ожидание сигнала с микрофона» вместо «нет сигнала»; флаг больше не мигает каждые полсекунды (ответ системы о заглушении держится до следующего опроса, снятие флага — после 0,2 с живого сигнала).
- Плавающие кнопки — в левом нижнем углу; через 5 с без касаний экрана бледнеют до 8 %.
- Кнопка выбора ноты без закреплённой ноты: диез нарисован по центру.

## 0.2.6 — 2026‑09‑16

- Замер шума комнаты убран (экран, шаг онбординга, пункт настроек, сохранённый профиль). Фоновый шум считается сам: 10‑й перцентиль тихих кадров между фразами за последние ~10 минут (первые секунды — быстро, дальше — по кадру в секунду тишины). Онбординг — 3 шага.
- «Вибрация» → «Виброотклик»; звезда на кнопке игры нарисована в размер значков паузы и настроек.
- «Обработка звука» в настройках без тега: состояние («не гарантирована» и т. п.) — в пояснении под «?».

- Верхней панели больше нет: управление — плавающие полупрозрачные круглые кнопки ★ (запуск/остановка игры), пауза и настройки. «Игровой режим» в настройках убирает кнопку ★. Во время игры время и очки (фиолетовым жирным) — справа в заголовке «Нота».
- Полноэкранный режим как в играх: системные панели скрыты, появляются свайпом от края.
- Подпись «серая линия — шум комнаты» убрана из заголовка спектра.
- Заголовок зоны ноты: без «не считается…» и «тап — закрепить ноту»; только «Цель: …» при закреплённой ноте (и «нет сигнала с микрофона»). Теги «живой/цель», «8 с», «dB» убраны.
- Центы стоят на месте: число по центру шкалы в поле под самое широкое значение, знак ¢ сразу за ним, международное имя слева.
- «Дать тон» не теряет обводку после первого выбора ноты.
- «Горб» в текстах заменён на «возвышение».
- Спектрограмма сжимается и растягивается по времени щипком (2…30 с), и вживую, и на паузе, плавно, без обрывов жеста; на паузе одним пальцем листается назад.
- В заголовке спектрограммы нет «лог 80 Гц – 8 кГц» — частоты подписаны на оси.
- Без голоса купол показывает «0 % · 0,0 dB» вместо «—»: подписи не меняют ширину.

## 0.2.5 — 2026‑09‑16

- Диалоги выбора (настройки, нота для «Дать тон»): варианты в чипах с тонкой коричневой обводкой; в выборе ноты каждая октава в своей рамке потолще. Язык «системный», тема «системная».

## 0.2.4 — 2026‑09‑16

- Микрофон: сторожевой таймер при каждой второй попытке переоткрытия берёт другой источник (UNPROCESSED ↔ VOICE_RECOGNITION) — на OnePlus задекларированный UNPROCESSED молчал ~40 с после установки, пока VOICE_RECOGNITION работает сразу.
- Замер шума комнаты: цифровая тишина не засчитывается, отсчёт ждёт сигнала, на экране «Нет сигнала с микрофона — подождите».
- Ряд под куполом: пилюли больше не обрезаются (A♯4 + «Дать тон» умещаются, ряд выходит за колонку симметрично), символ ♯ вместо ноты, обводка «Дать тон» видна.

## 0.2.3 — 2026‑09‑16

- Постоянный ключ подписи: с этой версии release-APK подписан одним и тем же ключом (секреты репозитория), обновления ставятся поверх. Один раз ещё придётся удалить 0.2.2, подписанную ключом раннера.
- Под куполом ряд: маленькая кнопка с закреплённой нотой (или ♪) открывает выбор ноты C2…C6 (международное и русское имя); «Дать тон» всегда на месте, но активна только при закреплённой ноте; отступ от дуги.
- Экран шума комнаты: в шапке спектра больше нет «купол —», наезжавшего на легенду («шум комнатырку»); замер сам запускает микрофон, если он не был запущен; разрешение на микрофон перечитывается при каждом возврате на экран.

## 0.2.2 — 2026‑09‑16

- Микрофон: сторожевой таймер — при 2 с цифровой тишины или когда система «заглушила» клиента (`isClientSilenced`: чужое приложение держит микрофон или разрешение выдано только что) поток закрывается и открывается заново; если микрофон не открылся, попытки повторяются каждые 2 с; в зоне ноты — «нет сигнала с микрофона». Ответ на «штиль» при первом запуске на OnePlus.
- «Дать тон»: тембр зависит от регистра — в верхах больше негармоничности, ярче удар, три струны, быстрое двухфазное затухание; «дудочка» на ля¹ ушла.
- Тонкие настройки: «Сохранить лог» — журнал приложения (устройство, версия, микрофон, ошибки) в файл по выбору.
- «Дать тон» — сразу под цифрами купола.

## 0.2.1 — 2026‑09‑16

- «Дать тон»: при закреплённой ноте кнопка под куполом играет её через динамик (строй A4 из настроек) — не гудок, а фортепианная нота: негармоничные обертоны жёстких струн (B ≈ 0,0004), верхние обертоны гаснут быстрее нижних, удар молоточка (косинусная атака 15 мс + тихий шумовой удар), две струны с расстройкой ±1,5 ¢.
- Иконка без отдельной тонкой линии: блик лежит на самой дуге. Выбор языка на первом экране онбординга.

## 0.2.0 — 2026‑09‑16

Первый публичный релиз: https://github.com/nikallass/cupola/releases. Изменения после 0.1.0 (раунды замечаний владельца 15–16 сентября):

- Новая иконка (адаптивная): фиолетовый диск, золотой купол, точка и искра. Сборки и релизы — GitHub Actions. Контраст спектрограммы действует только на новые столбцы, уже нарисованное не перекрашивается.

- Определитель ноты: `PitchTracker` — кандидаты гребня (только реальные локальные максимумы, слияние по уточнённой частоте) + YIN с бонусом, октавное правило, непрерывность (±0.25 окт), удержание подтверждённой ноты 0.35 с при прыжке, поддержка трека по YIN; снята проверка «пик заметнее соседних бинов», отбраковывавшая голос в оркестре. Нота на экране — по среднему высоты за 400 мс с гистерезисом и удержанием при «хаосе». Офлайн-трасса трекера на WAV (`WavTraceTest`), логкат `CupolaTrace` в debug.
- Калибровка убрана. Вместо неё замер шума комнаты (3 с тишины → профиль по бинам, вычитается из спектра; экран, онбординг, настройки «Замерить / Забыть», долгий тап по дуге).
- Купол считается по доле энергии голоса в полосе и по возвышению над соседними участками (`ring = √(share·hump)`), без нормировки по громкости; гейты «громче калибровки» и «высота плывёт» сняты; очки идут от `ring ≥ 0.5`. Пороги доли/возвышения — в advanced-настройках.
- Дуга купола заливается с обоих концов к вершине; при смыкании (`ring ≥ 0.9`) вершина искрится, подпись зеленеет, телефон вибрирует. Под дугой — доля и возвышение; в шапке спектра — «купол N % · возвышение ±N dB».
- Телефоны: компактная шапка (< 420 dp), строки настроек в две строки, легенда спектра скрывается, короткий ландшафт (< 480 dp высоты) с дугой рядом с нотой и спектрограммой на всю высоту.
- 2026‑09‑16: контраст спектрограммы (степенная кривая: ниже — полутона, выше — только пики) — в настройках отображения.
- 2026‑09‑16: границы прямого тона / вибрато / качания / тремоло вынесены в тонкие настройки (`VibratoThresholds`).
- 2026‑09‑16: линии гармоник от одного уровня под номерами; свёрнутый заголовок ноты фиксированной высоты.
- 2026‑09‑16: спектр — вертикальные линии гармоник (2 px) и их номера у верхнего края идут от f0, сглаженной так же, как пики (подъём 120 мс, спад 270 мс), огибающая пиков убрана; свёрнутая зона ноты показывает саму ноту с ▲/▼ и зелёным при попадании.
- 2026‑09‑16: зоны сворачиваются по тапу на заголовок (в ландшафте свёрнутая нота открывает оба графика на всю ширину); спектр сглажен во времени (150/400 мс); нота меньше (70/58 sp), доля внутри дуги, dB под ней без слова «возвышение».
- Спектрограмма/спектр: фиксированный диапазон −90…−20 dBFS, сглаживание между кадрами, пунктиры гармоник, подпись «серая линия — шум комнаты»; индикатор блокирующего гейта в зоне ноты.

## 0.1.0 — 2026-09-15 — «Анализ»

Первая версия: экран «Анализ» по SPEC §15.

- Захват микрофона: `UNPROCESSED` при поддержке, иначе `VOICE_RECOGNITION`; 48 кГц float, без шумоподавления и AGC; индикатор состояния обработки в настройках.
- DSP (чистый JVM, 66 тестов на синтетике §10): собственная real-FFT, YIN, noise floor, трекер гармоник и счётчик обертонов, метрики купола (RingRatio / RingShare / PeakSPR, нормировка по громкости), медианная линия высоты (pitchSD, drift), вибрато/качание/тремоло, гейты и score `0.6·ring + 0.25·pitch + 0.15·steady`, калибровка, очки.
- Экран «Анализ»: шапка (голос, полоса, время, очки, Пауза, Старт/Стоп, настройки), зона «Нота» (RU/EN, центы, шкала, дуга купола, подсказки, зелёное свечение), спектрограмма 8 с с лог/лин шкалой, лентой купола, треком f0, линией цели и тиками гармоник, спектр с огибающей и номерами гармоник; портрет и ландшафт.
- Сессия: очки с анимацией, серии, подсказки «Громче ≠ звонче», «Нота плывёт», «В позиции»; сводка; foreground-service с уведомлением; вибрация по баллу.
- Пауза: заморозка показаний и прокрутка 60‑секундного буфера.
- Калибровка (тишина 2 с → «а» 4 с) с проверками шума и стабильности; хранение по типу голоса / своему диапазону.
- Настройки с пояснениями «?»: тип голоса и свой диапазон, A4, нотация, знаки, язык, тема, шкала, окно FFT, источник аудио, вибрация, анимация, подсказки, advanced (пороги, confidence, k, веса).
- Онбординг, RU/EN, светлая/тёмная темы, шрифты Manrope и IBM Plex Mono.

Ограничения версии — README «Известные ограничения» и `docs/device-notes.md`.
