# Vocal Biofeedback Trainer (Android) — продуктовая и техническая спецификация

> Рабочее название: **Купол** (англ. `Cupola`). Опенсорс, офлайн, без телеметрии.
> Документ предназначен для реализации в Claude Code. Разделы «Открытые вопросы» содержат решения по умолчанию — реализовывать их, если владелец не сказал иного.

---

## 0. Суть в трёх абзацах

Приложение слушает голос через микрофон телефона и в реальном времени показывает: спектрограмму, гистограмму спектра с отмеченными обертонами, определённую ноту (в русской и латинской нотации, с отклонением в центах), количество слышимых обертонов и относительную меру «звона» — долю энергии в полосе певческой форманты (singer's formant, ~2.4–3.4 кГц в зависимости от типа голоса).

Это **визуализатор вокальной тренировки**: его включают на подставку и занимаются своими обычными упражнениями, а экран показывает, что происходит со звуком. Игровые элементы — вспомогательные: когда звук звонкий, стабильный по высоте и не форсированный, экран зеленеет, телефон вибрирует сильнее, «выбиваются монетки» / бьёт зелёный фонтан, а по показателям («купол/полётность», «точность попадания в ноты», «опора», «чистота») копятся очки. Все пороги — **относительные, от личной калибровки**, потому что микрофон, дистанция и комната делают абсолютные значения бессмысленными (это подтверждают и литература, и референсный инструмент sing.suedeai.ai).

Отдельный режим — **«Удержи верхнюю ноту»**: восходящий паттерн, верхняя нота должна стоять на месте (без дрейфа, без спада громкости, без роста «продутости»).

Научная база: певческая форманта (Sundberg; Bloothooft & Plomp 1986), SPR — Singing Power Ratio (Omori et al. 1996; Watts et al. 2006), CPPS как мера периодичности, нормы вибрато (Prame 1994/1997; Nix et al. 2016), эффекты визуального биофидбека (Wilson et al. 2005/2008; Van Stan et al. 2017). Подробный отчёт — отдельный документ `RESEARCH.md` (приложить к репозиторию).

---

## 1. Цели и не-цели

### Цели
1. Реалтайм-анализ голоса на телефоне с задержкой отображения ≤ 60 мс.
2. Честная относительная метрика «звона», которая **не поощряет громкость, зажатость и «продутость»**.
3. Игровая обратная связь: визуальная (монеты/фонтан) + тактильная (вибрация ∝ качеству).
4. Определение ноты с точностью ≤ 5 центов на устойчивом гласном, отображение в двух нотациях.
5. Определение/ручное задание диапазона и типа голоса — от этого зависит полоса «звона».
6. Универсальность: любой пользователь любого типа голоса, любой язык интерфейса (RU/EN на старте).

### Не-цели (v1)
- Не учит технике и не даёт педагогических советов («опусти гортань», «пой в маску»). Приложение показывает измерения, а не методику.
- Не диагностирует заболевания голоса.
- Не отправляет аудио никуда. Никакой облачной обработки.
- Не пытается быть тюнером для инструментов.
- Не оценивает «звон» во время SOVT-упражнений (мычание, губная вибрация, «з-з-з», «р-р-р») — там он не измерим; для них отдельный режим (см. §6.5).

---

## 2. Пользователи

| Персона | Что нужно |
|---|---|
| Новичок с преподавателем классической школы (первичный) | Ловить и удерживать «позицию»/звон между уроками; простой экран; не перегружать цифрами |
| Продвинутый любитель / студент | Спектрограмма, LTAS, SPR, CPPS, вибрато, экспорт записей |
| Преподаватель | Показать ученику спектр на уроке; сравнение двух дублей |
| Разработчик-контрибьютор | Чистая архитектура DSP ↔ UI, тесты на синтетике, документация |

---

## 3. Платформа и стек

- **Android 8.0+ (API 26)**, целевой API — последний стабильный. Kotlin, Jetpack Compose.
- Аудио: `AudioRecord`, источник `MediaRecorder.AudioSource.UNPROCESSED` (проверять `AudioManager.getProperty(PROPERTY_SUPPORT_AUDIO_SOURCE_UNPROCESSED)`), fallback → `VOICE_RECOGNITION`. **Никогда** `MIC`/`DEFAULT` (AGC и шумодав убивают полосу 3 кГц). Не включать `NoiseSuppressor`/`AutomaticGainControl`.
- Sample rate 48 000 Гц (fallback 44 100), PCM float/16-bit, mono. Буфер = `getMinBufferSize()` × 4, чтение в отдельном потоке с `THREAD_PRIORITY_URGENT_AUDIO` в lock-free ring buffer.
- DSP: собственный модуль на Kotlin (`:core-dsp`, чистый JVM без Android-зависимостей, тестируемый на десктопе). FFT — JTransforms или своя radix-2. Опционально NDK/KissFFT, если профилирование покажет необходимость.
- Pitch: **pYIN/YIN** (по умолчанию) + **MPM** как альтернатива в настройках; интерфейс `PitchDetector` позволяет позже подключить CREPE-tiny/SPICE через TFLite/LiteRT без изменения остального.
- Гаптика: `Vibrator`/`VibratorManager`, `VibrationEffect` с amplitude control; fallback на паттерны при `!hasAmplitudeControl()`.
- Хранение: Room (сессии, метрики, калибровки); WAV-файлы записей в app-private storage; экспорт через SAF.
- Лицензия: **MIT** (открытый вопрос §12). Без Google Analytics/Firebase/Crashlytics. Крэши — опционально ACRA в файл.
- Сборка: Gradle KTS, CI на GitHub Actions (unit-тесты `:core-dsp` + сборка debug APK + релизный APK по тегу).

### Модули
```
:app            — Compose UI, навигация, DI (Hilt или ручной), гаптика, Room
:core-audio     — захват аудио (AudioRecord), ring buffer, выбор источника
:core-dsp       — FFT, pitch, метрики, калибровка, скоринг (без Android API)
:core-notation  — имена нот RU/EN, октавы, конвертация Гц ↔ MIDI ↔ центы
:core-testdata  — генераторы синтетических сигналов для тестов
```

---

## 4. Аудио-пайплайн

```
mic → AudioRecord (48k) → ring buffer
   → окно 2048 (43 мс) с hop 480 (10 мс), Hann
   → FFT → power spectrum (dB)
   → [Spectrogram] столбец в текстуру
   → [Histogram] полосы + пики
   → pitch (pYIN на окне 2048–4096) → f0, confidence
   → harmonic tracker: k·f0, k=1..N, поиск пика ±3% → уровни гармоник
   → метрики (§5) на скользящем окне 200–1000 мс
   → gate + score (§6.2) → UI + haptics
```

- Все метрики публикуются как `StateFlow<FrameMetrics>` с частотой 50–100 Гц; UI подписан через Compose.
- Спектрограмма: отдельный `Bitmap`/текстура, обновление по столбцам, рисование через `Canvas.drawBitmap`; при нехватке производительности — OpenGL ES. Диапазон по Y: 0–8 кГц (лог-шкала по умолчанию, лин. переключаема). Динамический диапазон 60 дБ, авто-нормализация по максимуму за последние 3 с.
- Порог тишины: RMS ниже noise floor + 10 дБ → метрики не считаются, индикаторы гаснут. Noise floor измеряется 1 с при старте и обновляется медленно (percentile 10 по RMS).

---

## 5. Метрики и формулы

Все спектры в power (лин.), уровни в dB (10·log10). `E[a–b]` — сумма power по бинам в полосе a..b Гц.

### 5.1. Высота, нота, отклонение
- `f0` — из pitch detector; принимается при `confidence ≥ 0.7` (настраиваемо).
- `midi = 69 + 12·log2(f0/A4)`, `A4 = 440` по умолчанию (настройка 415–466).
- `nearest = round(midi)`, `cents = 100·(midi − nearest)` ∈ (−50, +50].
- Стабильность: `pitchSD = SD(cents)` на окне 500 мс (≥ 1 период вибрато) по **медианной линии** (скользящая медиана 200 мс), чтобы вибрато не считалось нестабильностью.
- Дрейф: наклон линейной регрессии `cents(t)` за окно 2 с, в центах/с.

### 5.2. Обертоны (гармоники)
- Для k = 1..K (K = 40 или до 8 кГц): ищем пик в спектре в окне `k·f0 ± 3%`; уровень `H_k` в dB.
- Обертон **считается слышимым**, если `H_k ≥ noiseFloor(k·f0) + 12 dB` и `H_k ≥ H_1 − 50 dB`.
- `overtoneCount` = число слышимых гармоник k ≥ 2 (по русской традиции «обертон» = призвук выше основного тона; в настройках можно переключить на «включая основной тон»).
- Отображение: на гистограмме над каждым найденным пиком метка `k` (для k ≤ 16) и маленький тик; сумма — крупным числом «обертонов: N». Сглаживать N медианой за 300 мс.

### 5.3. «Звон» (певческая форманта)
Полоса зависит от типа голоса (центр ≈ по Müller et al. 2022, ширина ±400 Гц):

| Тип голоса | Центр, Гц | Полоса ring, Гц |
|---|---|---|
| Бас | 2400 | 2000–2800 |
| Баритон | 2450 | 2050–2850 |
| Тенор | 2700 | 2300–3100 |
| Контральто/меццо | 2900 | 2500–3300 |
| Сопрано | 3100 | 2700–3500 |
| Не задан | 2800 | 2400–3200 |

Формулы:
- `RingRatio_dB = 10·log10( E[ring] / E[300–2000] )`
- `PeakSPR_dB = maxPeak_dB[2000–4000] − maxPeak_dB[0–2000]` (конвенция Omori: ближе к 0 — лучше; обученные ≈ −16 dB).
- `RingShare_% = 100 · E[ring] / E[80–8000]` — «как на сайте», только для отображения.
- **Нормировка по громкости** (Bloothooft & Plomp: +16–19 dB форманты на +10 dB SPL): `RingRatio_norm = RingRatio_dB − k·(SPL − SPL_baseline)`, `k = 0.7` по умолчанию (настраиваемо 0.5–0.9), `SPL` = RMS в dBFS.

### 5.4. Периодичность / качество
- `CPPS` — cepstral peak prominence smoothed (окно 40 мс, hop 10 мс, сглаживание по 10 кадрам и 10 квефренси-бинам, регрессия по кепстру 1–20 мс). Гейт против «продутости».
- `HNR` — из автокорреляции (Boersma) как дублирующая метрика.
- `H1−H2` — разница уровней 1-й и 2-й гармоник (низкое → pressed, высокое → breathy).
- `SpectralTilt` — наклон регрессии уровней гармоник (dB/окт.) для k=1..10.

### 5.5. Вибрато
- По контуру `cents(t)` за 1–2 с: вычесть скользящую медиану, FFT контура → `vibRate` (Гц) и `vibExtent` (± центы).
- Классификация (только информационно): 4.5–7 Гц «вибрато»; < 4 Гц и > ±120 центов «качание»; > 7.5 Гц «тремоло»; экстент < ±15 центов «прямой тон».

### 5.6. Нагрузка (опционально, как у референса)
- `voicedSeconds` за день; `cycleDose = Σ f0·Δt` (число колебаний связок). Показывать в истории, не на экране тренировки.

---

## 6. Режимы

### 6.1. Анализ (главный экран)
Сверху вниз (портрет):
1. **Нота**: крупно `Ля¹ / A4`, стрелка/шкала −50…+50 центов, цвет: зелёный |cents| ≤ 10, жёлтый ≤ 25, красный >25 (пороги настраиваемы). Под ней `f0 = 440.3 Гц`.
2. **Обертонов: N** и мини-полоска RingShare.
3. **Спектрограмма** (прокрутка влево, 8 с), полоса ring подсвечена полупрозрачной лентой.
4. **Гистограмма спектра** (лог-частота по X, dB по Y), метки гармоник `1,2,3…`, полоса ring выделена цветом, линия огибающей (LPC или пиковая интерполяция).
5. Кнопки: запись/стоп, тип голоса, калибровка, «тренировка».

Advanced-панель (свайп/переключатель): RingRatio, PeakSPR, CPPS, HNR, H1−H2, Tilt, vibRate/vibExtent, SD/дрейф — числами с мини-графиками за 10 с.

### 6.2. Тренировка (основной рабочий экран)
Экран, который стоит перед пользователем во время его обычных упражнений (попевки, вокализы, отдельные ноты). Приложение ничего не задаёт — пользователь поёт своё; экран показывает купол-индикатор, ноту и **зеленеет**, когда звук попадает в «правильную область» (звонко, стабильно, без нажима). Игровые элементы (монеты, фонтан, очки, вибрация) — поверх этого, отключаемы по одному.

Очки считаются по нескольким показателям (§6.2.1); центральный из них — купол. Условия, при которых купол-показатель вообще засчитывается — **все одновременно**:
- голос есть (RMS > noiseFloor + 10 dB), `confidence ≥ 0.7`;
- `pitchSD ≤ 20 центов` (окно 500 мс);
- `CPPS ≥ CPPS_baseline − 1 dB` (гейт против breathy);
- `H1−H2 ≥ H1H2_baseline − 6 dB` (мягкий гейт против pressed; не блокирует, а режет score на 50 %);
- `SPL ≤ SPL_baseline + 12 dB` (гейт против форсирования; выше — score = 0 и подсказка «тише»);
- режим SOVT не выбран.

#### 6.2.1. Показатели и очки
Четыре показателя, каждый 0…1, считаются покадрово и сглаживаются `attackRelease(attack = 80 мс, release = 400 мс)`:

| Показатель | Певческое имя | Формула (raw) |
|---|---|---|
| `ring` | Купол / полётность | `clamp((RingRatio_norm − RingRatio_baseline) / targetGain, 0, 1)`, `targetGain = 6 dB` (растёт: 3 сессии подряд средний ring > 0.8 → +1 dB). 0, если не выполнены гейты выше |
| `pitch` | Точность попадания в ноту | `1 − clamp((|cents| − 5) / 25, 0, 1)` — полный балл до ±5 ¢, ноль от ±30 ¢; считается к ближайшей ноте темперированного строя (в режиме удержания — к заданной ноте) |
| `steady` | Опора / ровность | `1 − clamp(pitchSD / 30, 0, 1)` с окна 500 мс по медианной линии (вибрато не штрафуется) |
| `clean` | Чистота | `clamp((CPPS − (CPPS_baseline − 3)) / 4, 0, 1)` |

- `score = 0.5·ring + 0.2·pitch + 0.15·steady + 0.15·clean` — веса в advanced-настройках; при выключенном куполе (SOVT-режим) `ring` исключается и веса перенормируются.
- **Зелёная область**: экран плавно зеленеет пропорционально `score`, полностью зелёный при `score ≥ 0.7`. Цвет — основной фидбек; всё остальное можно выключить.
- Монеты: каждые 250 мс при `score ≥ 0.5` вылетает монета, размер ∝ score; при `score ≥ 0.85` — зелёный фонтан. Сумма монет за сессию → история.
- Серия: непрерывные 3 с `score ≥ 0.7` → ×2, звук «дзинь» (отключаемо; по умолчанию звук выключен, чтобы не мешать микрофону).
- В сводке сессии каждый показатель отдельно: средний, лучший, доля времени ≥ 0.7 — чтобы было видно, что именно растёт (купол при той же громкости, точность, ровность).

Гаптика:
- `hasAmplitudeControl`: непрерывная волна сегментами 100 мс, амплитуда `= 30 + 225·score` (0, если score < 0.2). Каждый сегмент начинается/заканчивается с 0 для LRA.
- Иначе: импульсы 40 мс с периодом `= 600 − 500·score` мс.
- Отключение в настройках; автоматическое отключение, если телефон лежит на резонирующей поверхности не предусматривается — предупредить в онбординге (вибрация может попасть в микрофон; держать телефон в руке или на мягком).

Антипаттерны, которые приложение обязано пресекать текстом на экране:
- SPL растёт, RingRatio_norm не растёт → «Громче ≠ звонче. Попробуй тише».
- CPPS падает при росте ring → «Много воздуха».
- pitchSD растёт → «Нота плывёт — сначала стабильность».

Частота фидбека (guidance hypothesis): режим «Свободно» (постоянно) и «Итог» (экран гаснет на 10 с фразы, потом сводка). По умолчанию — «Свободно» для новичка первые 5 сессий, потом приложение предлагает «Итог».

### 6.3. «Удержи верхнюю ноту» (опора)
- Пользователь выбирает паттерн: 1-3-5-3-1, 1-5-1, 1-3-5-8-5-3-1, восходящая гамма до N ступеней; тональность и темп; верхняя нота удерживается T = 3–6 с.
- Приложение проигрывает референс (простой синус/пианино из сэмплов) или молчит (настройка). Микрофон и воспроизведение одновременно — использовать наушники или заглушать анализ на время референса.
- На удержании измеряется: `drift` (центы/с), `pitchSD`, спад RMS (dB/с), изменение CPPS, `H1−H2`.
- «Держится» = |drift| ≤ 5 центов/с, pitchSD ≤ 20, спад RMS ≤ 2 dB/с, CPPS не упал > 2 dB. Отображение — коридор ±25 центов, шарик ноты внутри, зелёная заливка за время в коридоре; вибрация короткими тиками при выходе из коридора (инверсный фидбек, отключаемо).
- Итог фразы: «верхняя нота ушла вниз/вверх на X центов за T с», график f0 и RMS.

### 6.4. Диапазон и тип голоса
- **Тест диапазона**: «спой самую низкую комфортную ноту → самую высокую комфортную → глиссандо вниз-вверх». Берутся нижний 5-й и верхний 95-й перцентиль стабильных f0 (confidence ≥ 0.8, ≥ 300 мс). Результат: `Ре большой — Ля¹ / D2–A4`, ширина в полутонах.
- **Оценка типа** — только по диапазону и тесситуре (медиана f0 в тесте), таблица ориентиров: бас ≈ E2–E4, баритон ≈ A2–G4, тенор ≈ C3–C5, контральто/меццо ≈ F3–F5, сопрано ≈ C4–C6. Показывать как «похоже на: тенор (ориентировочно)», всегда с ручным переопределением. Дисклеймер: тип голоса определяет преподаватель; здесь он нужен только для выбора полосы звона.
- Ручное задание: тип из списка + при желании нижняя/верхняя нота диапазона (пикер нот в двух нотациях).
- От типа зависят: полоса ring (§5.3), тональности паттернов по умолчанию, диапазон оси спектрограммы.

### 6.5. Разминка (SOVT)
- Пресеты: мычание, губная вибрация, «з-з-з», «р-р-р», произвольная попевка. В этом режиме **ring не считается и очки не даются**; показываются только нота/центы, стабильность, RMS и спектрограмма. Для губной/язычной вибрации детектор AM-модуляции (20–35 Гц) показывает индикатор «вибрация ровная».

### 6.6. Калибровка (baseline)
Обязательна перед первой тренировкой; можно переснимать; хранится по устройству и типу голоса.
1. Тишина 2 с → noise floor.
2. Гласный /а/ на комфортной ноте 4 с, обычной громкостью → `RingRatio_baseline`, `SPL_baseline`, `CPPS_baseline`, `H1H2_baseline`.
3. Опционально /э/ /и/ /о/ /у/ по 3 с → пер-гласная baseline (в тренировке можно выбрать гласный; иначе используется /а/).
4. Инструкция: телефон в руке или на подставке на расстоянии ~30 см, всегда одинаково; не менять расстояние между калибровкой и тренировкой.

---

## 7. Нотация (`:core-notation`)

- Латиница (научная): `C4 = 261.63 Гц` (до первой октавы). Диезы `C#4`, бемоли по настройке.
- Русская: имена `до ре ми фа соль ля си`, знаки `♯/♭` (или «диез/бемоль» текстом в accessibility). Октавы:

| MIDI-октава (научн.) | Русское название | Пример |
|---|---|---|
| 0 | субконтроктава | C0 — до субконтроктавы |
| 1 | контроктава | C1 — до контроктавы |
| 2 | большая | C2 — до большой |
| 3 | малая | C3 — до малой |
| 4 | первая | C4 — до первой (до¹) |
| 5 | вторая | C5 — до² |
| 6 | третья | C6 — до³ |
| 7 | четвёртая | C7 — до⁴ |
| 8 | пятая | C8 — до⁵ |

- Компактный формат: `Ля¹` / `Ля большой` / `ля малой`; полный формат в подписи: «ля первой октавы». Пользователь выбирает: только RU / только EN / обе (по умолчанию обе: `Ля¹ · A4`).
- Отклонение: `+12 ¢` / `−31 ¢` и словами «выше/ниже» в accessibility.
- Транспонирующие настройки не нужны. Строй A4 настраиваемый.

---

## 7a. Глоссарий приложения и подсказки при срабатывании гейтов

### Правило двух слоёв
Каждое понятие в приложении имеет **певческое** имя (слово из академической традиции, понятное преподавателю и ученику) и **акустическое** имя (что реально измеряется). Новичок видит певческий слой; акустический — в advanced-панели и в тултипах «что это измеряет».

Жёсткие ограничения:
- Певческие термины — только как **имена метрик и образы**. Приложение **не даёт технических инструкций** («опусти гортань», «провали на живот», «дави струной»): любая подсказка описывает, что произошло со звуком, и предлагает *откатиться назад / сделать тише / остановиться*, а не «сделать приём».
- Все образы помечены в тултипе: «образ из вокальной традиции; как его понимать — спросите преподавателя».
- Подсказки короткие (≤ 60 символов на экране тренировки), одна за раз, не чаще чем раз в 3 с, приоритет — по порядку в таблице гейтов.
- Тексты хранятся в `strings.xml` с ключами `glossary.<term>.*` и `hint.<gate>.*`, чтобы локализовать и править без кода.

### Глоссарий

| Ключ | Певческий термин | Акустика (что измеряем) | Тултип «что это» | Показывать новичку |
|---|---|---|---|---|
| `cupola` | Купол / полётность / звон | Певческая форманта; `RingRatio_norm`, `RingShare`, `PeakSPR` | Устойчивая яркая полоса около 2.4–3.4 кГц (по типу голоса). Даёт голосу «лететь» через оркестр без крика. Число — только относительно вашей калибровки. | Да (полоска/дуга) |
| `position` | Позиция / «со лба» / «звук вперёд» | Купол + стабильность высоты (`pitchSD`) при нормальной громкости | Не отдельная величина: звонко, стабильно и без нажима одновременно. | Да (как состояние «в позиции») |
| `support` | Опора | Удержание: `drift`, спад RMS, `CPPS`, `H1−H2` за время удержания | Нота стоит на месте, не садится и не «продувается» к концу. | Да (режим «Удержание») |
| `cover` | Прикрытие | Смещение формант гласного (А→О, Е→ЕИ) на верхних нотах; в v1 не измеряется, только пер-гласная калибровка | Академический способ петь верх без крика. Метрики нет — смотрите спектрограмму. | Только тултип |
| `pressed` | Зажим / «давить» / гулкий, «надутый» звук | Рост SPL без роста купола; низкий `H1−H2`; растёт `pitchSD`; вибрато исчезает или ускоряется | Горло сжато, звук сидит внутри. Громко ≠ звонко. | Да (подсказка) |
| `breathy` | Продутость / «сип» / много воздуха | Низкий `CPPS`/`HNR`, высокий `H1−H2`, шум между гармониками | Связки смыкаются неплотно, воздух уходит мимо звука. | Да (подсказка) |
| `pushed` | Форсирование / крик | `SPL > SPL_baseline + 12 dB` | Купол растёт от громкости, а не от позиции — очки не начисляются. | Да (подсказка) |
| `flat_sharp` | Нижé / вышé | `cents` < −25 / > +25 | Нота ниже/выше ближайшей на N центов. | Да |
| `drift` | Нота «плывёт» / «улетает» / «садится» | `drift` центы/с, `pitchSD` | Высота уходит во время удержания. | Да |
| `vibrato` | Вибрато / качание / тремоло | `vibRate`, `vibExtent` | 4.5–7 Гц — вибрато; медленнее и шире — качание; быстрее — тремоло. | Advanced |
| `overtones` | Обертоны | Слышимые гармоники `k·f0`, `overtoneCount` | Сколько призвуков выше основного тона различимо в спектре. | Да |
| `sovt` | Разминка с прикрытым ртом (мычание, губы, з-з-з, р-р-р) | Режим без оценки купола | Здесь купол не измерим по определению — смотрите ровность и высоту. | Да |
| `soft` | «Мягко, не кричите» | Общий принцип скоринга: тихий звонкий звук ценнее громкого гулкого | Приложение никогда не награждает громкость. | Онбординг |

### Подсказки при срабатывании гейтов (`hint.*`)

Приоритет сверху вниз; показывается первая сработавшая.

| Гейт (§6.2) | Ключ | Экран тренировки (кратко) | Развёрнуто (по тапу) |
|---|---|---|---|
| Горло/усталость — сообщает сам пользователь кнопкой «горло устало» | `hint.fatigue` | «Стоп. На сегодня хватит.» | Сессия закрывается, очки сохраняются, следующая тренировка доступна через 2 ч (можно отключить). Усталость и першение — сигнал закончить, а не «дожать». |
| `SPL > baseline + 12` | `hint.pushed` | «Громче ≠ звонче. Тише.» | Купол вырос от громкости, не от позиции. Вернитесь к громкости калибровки — очки начисляются только там. |
| `pitchSD > 20` | `hint.drift` | «Нота плывёт. Сначала стабильность.» | Пока высота гуляет, купол не считается. Спойте ноту тише и ровнее; если не держится — шаг назад: мычание. |
| `CPPS < baseline − 1` | `hint.breathy` | «Много воздуха.» | Звук продувается: смыкание неплотное. Не «поджимайте» — попробуйте меньше вдоха и мягче начало звука. |
| `H1−H2 < baseline − 6` (score ×0.5) | `hint.pressed` | «Похоже на зажим.» | Громкость есть, звона нет, звук сидит внутри. Не дожимайте — откатитесь на шаг назад (мычание или губы) и найдите позицию заново. |
| Купол растёт при падении CPPS | `hint.ring_but_breathy` | «Звон есть, но с сипом.» | Полоса ярче, но периодичность падает — это не позиция. Вернитесь к тихому чистому звуку. |
| SOVT-режим, попытка включить купол-скоринг | `hint.sovt` | «В разминке купол не считаем.» | Мычание и губы по определению не дают форманту. Разомнитесь и переходите к упражнениям с открытым звуком. |
| `score ≥ 0.7` три секунды подряд | `hint.good` | «В позиции. Не добавляй.» | Изнутри правильный звук часто кажется «маленьким» — это нормально. Не наполняйте его нажимом; запишите и послушайте снаружи. |
| Конец сессии, средний score вырос, а SPL — нет | `hint.progress` | «Звонче при той же громкости — это и есть рост.» | — |

Принципы формулировок: без оценок личности, без «неправильно», без техники; каждая подсказка называет *что видно в звуке* и предлагает одно из трёх: тише, шаг назад, стоп.

---

## 8. Экраны и навигация

1. **Онбординг** (первый запуск): разрешение микрофона; объяснение «всё относительно, сравнивай с собой»; выбор языка; тип голоса (тест или вручную, можно пропустить → «не задан»); калибровка.
2. **Анализ** (главный, §6.1).
3. **Тренировка** (§6.2) — крупный экран: фон зеленеет по score, индикатор-купол (дуга, заполняющаяся снизу вверх), нота крупно с отклонением, монеты/фонтан. Четыре показателя — мелкими полосками внизу. Никаких дБ и Гц.
4. **Удержание** (§6.3).
5. **Разминка** (§6.5).
6. **История**: сессии, средний score, лучшие серии, дневная нагрузка; графики прогресса RingRatio_norm по дням (медиана по сессии); записи (WAV) с воспроизведением и спектрограммой; сравнение двух записей (две спектрограммы + LTAS наложением).
7. **Настройки**: тип голоса/диапазон, калибровки, A4, нотация, гаптика (вкл/сила), пороги (advanced), окно FFT (2048/4096), pitch-алгоритм, лог/лин шкала, тема, экспорт/импорт данных (JSON + WAV zip), «удалить всё».
8. **О приложении**: лицензия, ссылка на репозиторий, `RESEARCH.md` кратко, дисклеймер «не медицинский инструмент, не заменяет преподавателя».

Дизайн: тёмная тема по умолчанию (спектрограмма читается лучше), крупные элементы — экран будет стоять на подставке в 30–50 см. Ландшафт поддерживается на экране «Анализ».

---

## 9. Модель данных (Room)

```
VoiceProfile(id, name, voiceType, lowMidi?, highMidi?, a4Hz, createdAt)
Calibration(id, profileId, deviceId, vowel, noiseFloorDb, ringRatioDb, splDbfs, cpps, h1h2, createdAt)
Session(id, profileId, mode, startedAt, durationSec, voicedSec, cycleDose, avgScore, coins, bestStreakSec, notes)
SessionFrame(sessionId, tMs, f0, cents, ringRatioNorm, cpps, spl, score)   // децимировано до 10 Гц
Recording(id, sessionId, path, durationSec)
Setting(key, value)
```

---

## 10. Тестирование

- `:core-dsp` unit-тесты на синтетике (`:core-testdata`):
  - синус 220 Гц → нота A3, cents ≈ 0 ± 1; 226 Гц → +46 ¢.
  - пилообразный сигнал 200 Гц + шум → overtoneCount растёт с падением шума; гармоники находятся на k·f0.
  - два сигнала с одинаковым спектром, разной громкостью → `RingRatio_norm` равен в пределах 1 dB.
  - сигнал с усиленной полосой 2.7 кГц (+10 dB) → RingRatio выше на ≈ 10 dB.
  - вибрато 5.5 Гц ±70 ¢ → vibRate 5.5 ± 0.3, extent 70 ± 10, pitchSD по медианной линии < 10.
  - белый шум → confidence < 0.5, метрики не считаются.
  - глиссандо +50 ¢/с → drift 50 ± 5.
- Инструментальные: захват из `UNPROCESSED` vs `VOICE_RECOGNITION` на 3 устройствах, сравнение спектра тестового сигнала из динамика ноутбука; замер латентности (генератор → микрофон → отрисовка) ≤ 60 мс.
- Ручной чек-лист: смартфон на столе vs в руке (вибрация в микрофон); низкие мужские ноты (~80 Гц) не дают октавных ошибок; сопрано на C6 — pitch держится.

---

## 11. Этапы

**M1 — Анализ (MVP).** Захват, спектрограмма, гистограмма с обертонами, нота в двух нотациях, RingShare/RingRatio без гейтов, тип голоса вручную, настройки базовые. Критерий: стабильная работа 10 мин без дропов кадров на среднем устройстве 2021 г.

**M2 — Калибровка + Тренировка.** Baseline, гейты, четыре показателя и score, зелёный фон, монеты/фонтан, гаптика с fallback, история сессий, антипаттерн-подсказки.

**M3 — Удержание + Диапазон.** Паттерны, референс-тон, метрики удержания, тест диапазона, авто-оценка типа.

**M4 — Продвинутое.** Advanced-панель, LTAS, сравнение записей, пер-гласная калибровка, экспорт, EN-локализация, режим «Итог».

**M5 — Экосистема.** Подключаемые pitch-модели (CREPE-tiny/SPICE), плагины упражнений в JSON, F-Droid-сборка.

---

## 12. Открытые вопросы и решения по умолчанию

| Вопрос | По умолчанию (если владелец не сказал иного) |
|---|---|
| Лицензия | MIT |
| DI | Hilt |
| Минимальный API | 26 |
| Поддержка iOS/KMP | Нет; но `:core-dsp` пишется без Android-зависимостей на случай KMP |
| Референс-тон в «Удержании» | Синус с мягкой огибающей, проигрывается только до начала пения; во время анализа молчит |
| Звуковые эффекты | Выключены по умолчанию |
| Хранение WAV | Только по кнопке «записать», не автоматически |
| Тип голоса «не задан» | Полоса 2400–3200 Гц |
| Пороги гейтов | Как в §6.2; все — в advanced-настройках с кнопкой «сбросить» |
| Название/иконка | «Купол», стилизованная дуга |

---

## 13. Известные ограничения (написать в README и в «О приложении»)

- Все метрики относительные: другой телефон, расстояние или комната — новая калибровка. Нельзя сравнивать числа с другими людьми.
- Певческая форманта формируется месяцами; отсутствие роста за неделю — норма.
- Высокий «звон» можно получить и зажатым звуком; приложение частично отсекает это гейтами, но окончательный судья — преподаватель и ощущение свободы горла. Если горло устало или болит — прекратить занятие независимо от очков.
- Твэнг/носовой звук тоже поднимает энергию около 3 кГц; приложение не отличает его надёжно.
- На части устройств `UNPROCESSED` отсутствует, а `VOICE_RECOGNITION` реализован с отклонениями от спецификации; в настройках есть переключатель источника и индикатор «обработка отключена / не гарантирована».

---

## 14. Ссылки на исследования (кратко; полный список в `RESEARCH.md`)

- Omori et al. 1996, J. Voice 10(3) — Singing Power Ratio.
- Watts et al. 2006, J. Voice 20(1) — SPR у обученных/необученных.
- Sundberg 2001, J. Voice 15(2) — уровень и центр певческой форманты.
- Bloothooft & Plomp 1986, JASA 79(6) — зависимость форманты от SPL и гласной.
- Müller et al. 2022, PMC9605961 — центры форманты по типам голоса.
- Prame 1994/1997, JASA; Nix et al. 2016, J. Voice — нормы вибрато.
- Murton, Hillman & Mehta 2020, AJSLP — CPPS.
- Wilson, Thorpe & Callaghan 2005/2008 — визуальный фидбек в обучении пению.
- Van Stan et al. 2017 — принципы моторного обучения в голосовом биофидбеке.
- Kim et al. 2018 (CREPE); SPICE 2019; TarsosDSP — pitch-алгоритмы.
- Android: MediaRecorder.AudioSource, Haptics APIs, Oboe.

---

## 15. v0.1 «Анализ» — решения ревью 2026-09-15 и визуальная спецификация

Этот раздел имеет приоритет над предыдущими там, где они расходятся. Макет: `design/mock-analysis-v0.1.html` (живой: https://claude.ai/artifact/D49wd7exrmsNzbdTbcNnLV).

### 15.1. Границы версии 0.1

**Входит:** экран «Анализ» целиком (§15.3), онбординг, калибровка (baseline), настройки, RU+EN, светлая/тёмная тема, портрет + ландшафт, обертоны на спектрограмме и спектре, вибрато-анализ (§5.5), сессия с очками и подсказками, гаптика (fallback-режим), foreground-service для продолжения анализа в свёрнутом виде, окно FFT 2048/4096 в настройках.

**Не входит (backlog):** «Тренировка», «Удержание», «Разминка», «История» и Room, advanced-панель (CPPS, HNR, H1−H2, Tilt), запись WAV / «поделиться», адаптивные фильтры «как в FPV» (динамические notch, бланкирование вибро), экспорт сессии учителю, MPM/pYIN, CI/публикация, F-Droid, мультипрофиль (не планируется; вместо него позже импорт/экспорт).

### 15.2. Инфраструктура и инженерные решения

- Пакет `ru.dvedev.me.cupola`, название «Купол». Git локально, без публикации; коммиты на английском; тег `v0.1.0`.
- Сборка на сервере `217.60.62.102` (`/root/cupola`, JDK 17, SDK 35): `scripts/deploy.sh` = rsync → `gradlew :app:assembleDebug` → scp APK → `adb install` на KENSHI E11 (`192.168.0.16:5555`, Android 13, MT8781, 1200×2000 @ 240 dpi, вибромотор без amplitude control).
- Ручной DI (без Hilt). DataStore Preferences для настроек; Room не подключается. Собственная radix-2 real-FFT. Pitch: YIN за интерфейсом `PitchDetector`; пункт «алгоритм» в настройках скрыт до появления второй реализации.
- Базовый noise floor (RMS и по бинам, §4/§5.2) остаётся; продвинутые фильтры — backlog.
- Формат сессии (кадры метрик 10 Гц + WAV) проектируется в `:core-dsp` сейчас как модель данных, без персистентности.

### 15.3. Экран «Анализ» — структура

Экран — единая панель без внешних отступов; четыре зоны разделены 1-px линиями, у каждой зоны своя шапка-полоска (`panel2` 60 %). Никаких карточек-в-карточках, теней, скруглений внутри экрана.

**Портрет (сверху вниз):**
1. **Шапка**: слева `Купол · <тип голоса> · <полоса купола>`; справа время сессии, очки (золотом), кнопка «Пауза» (контурная), кнопка «Старт» (заливка violet-ink; в состоянии «Стоп» — приглушённая контурная), шестерёнка настроек.
2. **Нота** (≈ 1.5× от первоначального; главная): `Соль¹ · G4 · +4 ¢` (нота Manrope 700 ~84 dp, латиница и центы — IBM Plex Mono), шкала −50…+50 ¢ с зелёной зоной ±10 и тиками ±25, маркер цвета попадания; строка `392.4 Гц · обертонов 14 · вибрато 5.6 Гц ±58 ¢ · цель …`; справа дуга «купол» (заливка ∝ ring, число под ней); внизу **постоянно зарезервированная** строка подсказки (не сдвигает содержимое). Тап по ноте закрепляет её как цель.
3. **Спектрограмма**: лог-ось 80 Гц–8 кГц (метки 100…6.4k), 8 с на экране, ось времени (−8…0 с), лента купола (золото 10 % + пунктирные края + подпись «купол»), линия f0 цветом попадания, при закреплённой цели — фиолетовая линия цели (90 %) и её гармоники (30 %), у правого края — тики слышимых гармоник с номерами `k ≤ 16`. Колормап крем → золото → чёрный (тёмная тема — инверсия), сглаженная отрисовка. Занимает всё оставшееся место.
4. **Спектр**: лог-ось X как у спектрограммы, dB по Y с сеткой (−20…−80), полоса купола, кривая с золотой заливкой, пунктирная сглаженная огибающая по гармоникам, номера над пиками (без наложений), пунктир noise floor; в шапке — `купол +3.1 dB к калибровке`. Высота ≈ 270 dp.

**Ландшафт:** шапка во всю ширину; левая колонка 360 dp — «Нота»; правая — спектрограмма (~60 % высоты) над спектром (~40 %). Всё рабочее содержимое влезает в экран без прокрутки.

**Зелёный:** зеленеет только зона «Нота» (наложение `ok` с прозрачностью ∝ score/0.7, до ~32 % в сессии, до ~12 % вне сессии).

**Очки:** начисляются в сессии при `score ≥ 0.5`; интервал начисления `0.6 − 0.48·ring` с (чем больше доля звука в куполе, тем чаще), размер порции `1 + round(2·ring)`; визуально — золотые кружки из дуги купола (зелёные при `score ≥ 0.85`). Слово «монеты» не используется — только «очки».

**Пауза:** отдельная кнопка; замораживает графики, позволяет проскроллить 60-секундный буфер; анализ продолжается в фоне буфера.

### 15.4. Палитра и шрифты

Светлая тема (по suedeai): фон/панель `#fffaf2`, `panel2 #efe6d5`, линии `#ddd4c4`/`#c9bda0`, текст `#20201d`, приглушённый `#5c564d`, подписи `#6b6455`; золото `#c59642` (купол, спектр, очки) и `#82631f` (текст-золото); фиолетовый `#6d28d9` (кнопка «Старт», бейджи), `#8b5cf6` (линия цели); статусы: ok `#2a6f53`, warn `#82631f`, bad `#9d3f33`. Тёмная тема: панель `#24231f`, `panel2 #2e2c27`, линии `#3b382f`/`#4d493f`, текст `#efe8da`/`#b5ac9c`/`#8c8474`, золото `#d6ab5a`/`#e2c07a`, фиолетовый `#b7a2fb`/`#a78bfa`, ok `#63b48e`, warn `#d1a24a`, bad `#d4705f`; колормап `#24231f → #d6ab5a → #faf4e8`. Светлая тема — по умолчанию.

Шрифты: Manrope (UI, нота), IBM Plex Mono (цифры, подписи, бейджи; `tabular-nums`), вшиты в APK. Бейджи и подписи — mono 10.5–11 dp, uppercase, tracking 0.1–0.12 em.

### 15.5. Логика v0.1

- Анализ идёт всегда, пока приложение открыто. «Старт» открывает сессию (очки, серии, подсказки); при сворачивании в сессии — foreground-service с уведомлением «нота + Стоп»; без сессии сворачивание останавливает микрофон. «Стоп» → сводка: средний/лучший score, доля времени ≥ 0.7, очки, лучшая серия, по показателям ring/pitch/steady.
- `score = 0.6·ring + 0.25·pitch + 0.15·steady`; гейты: голос (RMS > noise + 10 dB), `confidence ≥ 0.7`. **Раунд 3 (§15.6):** гейтов громкости и стабильности высоты нет — `pitchSD` влияет только на `steady`; `ring` считается по доле и возвышению, а не относительно базы. `clean`/CPPS — с advanced-панелью (backlog); веса — одна таблица в `:core-dsp`.
- Вибрато: `vibRate`/`vibExtent` по контуру центов 1–2 с; 4.5–7 Гц и ≤ ±120 ¢ — не штрафуется; качание (< 4 Гц или > ±120 ¢) и тремоло (> 7.5 Гц) снижают `steady`; дрейф медианной линии — отдельно (`drift`, ¢/с) и триггерит `hint.drift`. Подпись под нотой: «вибрато 5.6 Гц · ±58 ¢» / «качание» / «тремоло» / «прямой тон».
- Подсказки v0.1: `hint.drift` (pitchSD > 20 ¢), `hint.good`; одна строка, не чаще раза в 3 с (`hint.pushed` снят вместе с гейтом громкости, §15.6).
- ~~Калибровка обязательна перед первой сессией~~ — **снята (§15.6)**. Вместо неё необязательный замер шума комнаты: 3 с тишины → профиль по бинам, вычитается из спектра; шум > −45 dBFS — предупреждение; переснять — из настроек, онбординга и долгим тапом по дуге купола.
- Типы голоса: бас, баритон, тенор, контральто/меццо, сопрано, не задан (полосы §5.3) + **свой диапазон** (нижняя/верхняя граница, шаг 50 Гц, ширина ≥ 200 Гц). Кнопка «Замерить» (шум комнаты) — рядом с выбором типа в настройках.
- Гаптика (§15.6): вибрирует, пока половинки дуги купола сомкнуты (`ring ≥ 0.9`): импульсы 40 мс каждые 150 мс либо непрерывная мягкая волна при `hasAmplitudeControl`; по умолчанию вкл. на телефонах (`smallestWidth < 600 dp`), выкл. на планшетах.
- Онбординг: микрофон → «купол — доля и возвышение, не громкость; сравнивай с собой» → тип голоса → замер шума комнаты (можно позже). Язык — по системной локали, переключатель в настройках.
- Настройки (у каждого поля «?» с пояснением): тип голоса + свой диапазон + «Шум комнаты: Замерить / Забыть», A4, нотация RU/EN/обе, язык, тема, шкала лог/лин, окно FFT 2048/4096, источник аудио (авто/UNPROCESSED/VOICE_RECOGNITION) с индикатором «обработка отключена / не гарантирована», вибрация, очки-анимация, подсказки; свёрнутый «Advanced»: пороги центов, confidence, «купол: доля для максимума» (%), «купол: возвышение для максимума» (dB), границы вибрато (прямой тон до N ¢, вибрато от/до N Гц, размах до N ¢ — 2026‑09‑16), веса score, «сбросить».

### 15.6. Решения владельца 2026‑09‑15, раунд 3 (после прослушивания записей)

Имеют приоритет над §5.3, §6.2, §6.6 и §15.5 там, где расходятся.

- **Калибровки нет.** Личная база (`RingRatio_baseline`, `SPL_baseline`) и нормировка по громкости `k` убраны из пайплайна, настроек, онбординга и сводки. Гейты `NOT_CALIBRATED` и `PUSHED` сняты.
- **Купол — по возвышению и доле, не по громкости.** Для каждого кадра после вычитания профиля шума комнаты (по бинам, с запасом +3 dB над 10‑м перцентилем): `share = E[полоса] / E[80–8000 Гц]` (%), `hump = peak[полоса] − ½·(peak[lo−700…lo−100] + peak[hi+100…hi+900])` (dB). `ring_raw = √(share_credit · hump_credit)`, где `share_credit = clamp((share − share_full/6)/(share_full − share_full/6))`, `hump_credit = clamp((hump + hump_full)/(2·hump_full))`; по умолчанию `share_full = 12 %`, `hump_full = 6 dB` (обе — в advanced-настройках). Оба множителя обязательны: энергия в полосе без возвышения или возвышение без энергии дают 0. Чистое усиление сигнала не меняет ни `share`, ни `hump` (тест `pure gain leaves share and hump unchanged`).
- **Профиль шума комнаты** (`RoomNoise`): 3 с тишины → средняя мощность по бинам + медианный RMS; хранится в DataStore (один на устройство), при старте засевает `NoiseFloor` (`seed`), дальше адаптивный флор живёт как раньше. Без замера всё работает на адаптивном флоре.
- **Очки** идут от самого купола: пока `ring ≥ 0.5`, порция `1 + round(2·ring)` каждые `0.6 − 0.48·ring` с (было — от `score ≥ 0.5`).
- **Дуга купола** заливается с обоих концов к вершине (`½·180°·ring` с каждой стороны); при `ring ≥ 0.9` половинки «смыкаются»: вершина мерцает и искрится, подпись зеленеет, очки летят, телефон вибрирует. Под дугой — доля (%) и строкой ниже «возвышение ±N dB»; в шапке спектра — «купол N % · возвышение ±N dB».
- **Определитель ноты** — `PitchTracker` поверх YIN и гребенчатого поиска: кандидаты = локальные максимумы гребня по сетке 25 ¢ (гармоника засчитывается только как реальный локальный максимум спектра в допуске max(3 %, 0.6 бина)) + YIN и его октавы с бонусом `25·conf`; октавное правило (серия на 2–4‑й гармонике более низкого кандидата с ≥ половиной её счёта дисконтируется вдвое); на треке следуем лучшему кандидату в ±0.25 октавы, прыжок дальше принимается только после 0.35 с (подтверждённая нота, `strength ≥ 30`, удерживается всё это время с confidence 0.7); YIN в ±0.25 октавы от трека поддерживает трек, когда гребень ничего не видит. Проверка «пик заметнее соседних бинов» убрана — на оркестровой записи она отбраковывала голос рядом с громкими линиями.
- **Отображение ноты** — по среднему непрерывной высоты за 400 мс с гистерезисом 300 мс; при «хаосе» (ни одна нота не набирает половину кадров) показанная нота удерживается.
- **Телефоны**: шапка без бренда/полосы и с компактными кнопками при ширине < 420 dp; строки настроек складываются в две строки (контрол под заголовком, справа); легенда спектра прячется; горизонтальный телефон (высота < 480 dp) — колонка ноты 330 dp с дугой рядом с нотой, спектрограмма на всю высоту, спектр только при высоте ≥ 400 dp.
- **Замечания 2026‑09‑16 по скринам с телефона:** заголовки зон сворачивают зону по тапу (шеврон ▸/▾): можно раскрыть графики на весь экран или оставить одну ноту; в ландшафте свёрнутая нота становится полосой над обоими графиками, которые занимают всю ширину. Спектр сглаживается во времени (подъём 120 мс, спад 270 мс), а не покадрово. Нота 70 sp (58 sp в компактной колонке) — 84 sp срезалась сверху на телефоне. Доля (%) — внутри дуги купола, под дугой только «±N dB» без слова «возвышение»; то же в шапке спектра. Линии гармоник на спектре (2 px) и их номера у верхнего края идут от f0, сглаженной в лог-области с тем же подъёмом 120 мс, что и бины (спад бинов 270 мс), огибающая пиков и номера над пиками убраны. Свёрнутая зона ноты показывает в заголовке саму ноту с ▲/▼ (выше/ниже порога ok) и зелёным при попадании. Полоса купола остаётся золотой (фиолетовый вариант владелец отклонил как слишком грубый); все линии гармоник начинаются на одном уровне под рядом номеров; свёрнутый заголовок ноты одной высоты с текстом и без. Настройка «Контраст спектрограммы» (50…300 %, k = %/100): ниже 100 % — `level^k` (проступает даже зернистый шум), выше — полоса между порогом `t = 0.9·p·(1 − 1/k)` и бегущим пиковым уровнем `p` растянута до полной темноты, ниже порога белое (только жирные тёмные пики, независимо от абсолютного уровня устройства); подложка не тонируется (яркость владелец отклонил). Смена контраста не перекрашивает уже нарисованные столбцы — только новые. При закреплённой ноте под куполом — кнопка «Дать тон»: 2,4 с фортепианной ноты закреплённой ноты через динамик (аддитивный синтез: 14 негармоничных обертонов `k·f0·√(1+B·k²)`, B = 0,0004; затухание k‑го обертона `τ₁/(1+0,12·k^1,6)`, τ₁ = 1,1 с; косинусная атака 15 мс + тихий шумовой удар молоточка; две струны ±1,5 ¢), чтобы настроиться заново. «Подстройка строя» (advanced, −50…+50 ¢): `A4_eff = A4·2^(¢/1200)` для определителя ноты и для «Дать тон». Микрофон: сторожевой таймер в `AudioEngine` (2 с нулей или `isClientSilenced` → переоткрытие потока, не чаще раза в 4 с; флаг `inputSilent` → «нет сигнала с микрофона» в зоне ноты; неудачное открытие повторяется каждые 2 с). Тембр «Дать тон» зависит от регистра (B, яркость молоточка, число струн, затухание). «Сохранить лог» в advanced — logcat своего процесса с шапкой устройства в файл через системный диалог. Под куполом — ряд «нота (♪) · Дать тон»: пилюля с международным именем закреплённой ноты открывает выбор ноты C2…C6; «Дать тон» неактивна без закреплённой ноты. Переоткрытие микрофона чередует источники при AUTO; замер шума пропускает кадры с RMS < −150 dBFS и показывает «нет сигнала». Раунд 2026‑09‑16 (вечер): «Режим тренировки» в настройках (выкл. — нет верхней панели, плавающая кнопка настроек); в заголовке зоны ноты только «Цель: …»; теги зон убраны (кроме «пауза»); центы в фиксированных позициях (число по центру шкалы, ¢ за ним). Спектрограмма: щипок меняет видимое окно 2…30 с (кольцевой битмап на 30 с); без режима тренировки — плавающие «пауза» и «настройки».
